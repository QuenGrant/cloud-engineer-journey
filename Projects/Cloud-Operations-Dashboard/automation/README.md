## Section 26 — Python/Boto3 Automation

Completed Python automation using Boto3 to authenticate with AWS, discover the capstone EC2 instance, inspect its state, and start and stop the instance programmatically.

## Section 27 — AWS Resource Automation

Improved the Boto3 automation by using a reusable EC2 instance ID variable instead of repeating the resource ID directly in the API call.

## Section 28 — Automation Testing

Verified the AWS automation script runs successfully against the capstone environment after confirming AWS authentication.

Verified:
- Boto3 authentication
- EC2 automation
- Reusable resource identification
- Automation execution testing
- EC2 returned to stopped state for cost control
